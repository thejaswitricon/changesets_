{
  "name": "@monorepo-starter/root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@changesets/cli": "^2.26.1"
  },
  "scripts": {
    "release": "yarn build && changeset publish"
  }

}
